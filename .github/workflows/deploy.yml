# Deployment is handled by Cloudflare Pages direct Git integration.
# Cloudflare automatically runs `npx wrangler deploy` on every push to main.
# This file is kept for reference only — the workflow is disabled.

# name: Deploy to Cloudflare Pages
# on:
#   push:
#     branches: [main]
